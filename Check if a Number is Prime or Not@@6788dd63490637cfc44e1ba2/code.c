#include<stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    if (n > 1 && (n % i != 0) for all i from 2 to sqrt(n)){
    printf("Prime");}
    else{
    printf("Not Prime");}
    return 0;
}